
	var store = fixture.store(5, function(i){
		return {
			name: "<%= alias %> "+i,
			description: "<%= alias %> " + i
		}
	});
	
	fixture({
		'GET /<%= pluralAlias %>' : store.findAll,
		'GET /<%= pluralAlias %>/{id}' : store.findOne,
		'POST /<%= pluralAlias %>' : store.create,
		'PUT /<%= pluralAlias %>/{id}' : store.update,
		'DELETE /<%= pluralAlias %>/{id}' : store.destroy
	});

	return store;