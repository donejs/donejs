steal('can', '<%= appPath %>/models/<%= underscore %>.js', './init.ejs', 'jquery/dom/form_params',
	function (can, <%= Alias %>, initView) {

	/**
	 * @class <%= appPath %>/<%= underscore %>/create
	 * @alias <%=Alias%>Create
	 * @parent index
	 * @inherits jQuery.Controller
	 * Creates <%= plural %>
	 */
	return can.Control(
	/** @static */
	{},
	/** @Prototype */
	{
		init : function () {
			this.element.html(initView());
		},
		submit : function (el, ev) {
			ev.preventDefault();
			el.find('[type=submit]').val('Creating...')
			new <%= Alias %>(el.formParams()).save(function() {
				el.find('[type=submit]').val('Create');
				el[0].reset()
			});
		}
	});
});