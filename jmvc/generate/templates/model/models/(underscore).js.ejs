steal('can', function (can) {
	/**
	 * @class <%= module %>
	 * @alias <%= Alias %>
	 * @parent index
	 * @inherits can.Model
	 *
	 * Wraps backend <%= underscore %> services.
	 */
	return can.Model(
	/* @static */
	{
		findAll : "GET /<%= pluralAlias %>",
		findOne : "GET /<%= pluralAlias %>/{id}",
		create : "POST /<%= pluralAlias %>",
		update : "PUT /<%= pluralAlias %>/{id}",
		destroy : "DELETE /<%= pluralAlias %>/{id}"
	},
	/* @Prototype */
	{});
});