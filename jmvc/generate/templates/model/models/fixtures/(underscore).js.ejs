steal('can/util/fixture', function (fixture) {
	var store = fixture.make(5, function(i, <%= underscore %>){
		return {
			name: "<%= underscore %> "+i,
			description: "<%= className %> " + i
		}
	});

	fixture({
		'GET /<%= plural %>' : store.findAll,
		'GET /<%= plural %>/{id}' : store.findOne,
		'POST /<%= plural %>' : store.create,
		'PUT /<%= plural %>/{id}' : store.update,
		'DELETE /<%= plural %>/{id}' : store.destroy
	});
});