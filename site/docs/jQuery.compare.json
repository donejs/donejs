c({"name": "jQuery.compare", "type": "page", "comment": "<p><code>jQuery.compare</code> adds <code>[jQuery.fn.compare jQuery.fn.compare(element)]</code> which compares the position of two nodes and returns a number representing\na bitmask detailing how they are positioned relative to each other.</p>\n\n<pre><code>$('#foo').compare($('#bar')) //-&gt; Number\n</code></pre>\n\n<p>You can expect it to return the same results as \n[http://www.w3.org/TR/DOM-Level-3-Core/core.html#Node3-compareDocumentPosition | compareDocumentPosition].\nParts of this documentation and source come from [http://ejohn.org/blog/comparing-document-position | John Resig].</p>\n\n<p>The following list shows the <code>bitmask</code>, the <strong>number</strong> and what it corresponds to:</p>\n\n<ul>\n<li><code>000000</code> -> <strong>0</strong>: Elements are identical</li>\n<li><code>000001</code> -> <strong>1</strong>: The nodes are in different documents (or one is outside of a document)</li>\n<li><code>000010</code> -> <strong>2</strong>: #bar precedes #foo</li>\n<li><code>000100</code> -> <strong>4</strong>: #foo precedes #bar</li>\n<li><code>001000</code> -> <strong>8</strong>: #bar contains #foo</li>\n<li><code>010000</code> -> <strong>16</strong>: #foo contains #bar</li>\n</ul>\n\n<p>The returned number can also represent a bitmask matching multiple conditions. For example, if the returned number is <strong>20</strong>, which is the bitmask of <code>010100</code>, it means that #foo precedes #bar <em>and</em> #foo contains #bar.</p>\n\n<p>You can tests if the number returned by <code>$.fn.compare</code> matches any of these conditions by combining it with a <a href=\"https://developer.mozilla.org/en/JavaScript/Reference/Operators/Bitwise_Operators\">bitwise AND operator</a>:</p>\n\n<pre><code>if( $('#foo').compare($('#bar')) &amp; 2 ) {\n  console.log(\"#bar precedes #foo\")\n}\n\nif( $('#foo').compare($('#bar')) &amp; 8 ) {\n  console.log(\"#bar contains #foo\")\n}\n\nif( $('#foo').compare($('#bar')) &amp; 10 ) {\n  console.log(\"#bar precedes #foo and #bar contains #foo\")\n}\n</code></pre>\n\n<h2>Demo</h2>\n\n<div class='demo_wrapper' data-demo-src='jquery/dom/compare/compare.html'></div>", "parents": ["jquerypp"], "test": "jquery/dom/compare/qunit.html", "plugin": "dom/compare", "src": "jquery/dom/compare/compare.md", "children": []})